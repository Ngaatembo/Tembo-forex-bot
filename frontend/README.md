# Frontend — AI Forex Research Platform

Next.js dashboard. Not built out yet — Phase 9 in the roadmap.
Phase 0 ships a placeholder page and an API service wrapper only.

## Setup

```bash
cd frontend
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` if the backend isn't on
`http://localhost:8000`.
